{
  "name": "job-hunter",
  "private": true,
  "version": "0.0.1",
  "description": "Developer-focused job hunting assistant",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=10.0.0"
  },
  "packageManager": "pnpm@10.30.3",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "format": "turbo format"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  }
}
