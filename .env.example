# Database (used by FastAPI service)
DATABASE_URL=postgresql://jobhunter:jobhunter@localhost:5432/jobhunter

# OpenAI (used by FastAPI service — deferred to Phase 2)
OPENAI_API_KEY=

# Extractor toggle (set USE_LLM_EXTRACTOR=true to use OpenAI-based extraction instead of regex)
USE_LLM_EXTRACTOR=false

# Next.js public API URL
NEXT_PUBLIC_API_URL=http://localhost:8000
